# Kit 3 — Provenance log

Human-authorship chain for "Build Your First Classroom Tool."

| Stage | Date | Notes |
|---|---|---|
| Job decision | 2026-09-23 | Per `tpt/intel/2026-09-23-wed-brief.md` section (a): job 2 of the Wednesday routine's three-job decision. No PD was in progress (`kits/` held only kit01-tpt and kit02-tpt). This run builds the deck and facilitator script only, plus the lab's starter game file (the brief's own recommendation, to avoid script drift against a file built later). **Nothing ships to TPT this run.** The seven remaining KIT_STANDARD components (prep guide, participant handout, First 48 Hours, 30-day plan, exit ticket, admin one-pager, references) ship 2026-09-30 as drop 59. |
| Mandate statistics verified | 2026-09-23 | The brief's seven validation-table rows and its five cited mandate statistics were all flagged unverified web research from 2026-09-21. A dedicated research pass re-verified all five against primary sources (enrolled Maryland SB 720 text, Virginia/Ohio law summaries, the FutureEd legislative tracker, Gallup/Walton and IBM/Morning Consult 2026 surveys) before any figure reached a slide. Two corrections made: "70+ bills" → exact 77; Ohio recast as a policy-adoption mandate only, not a training mandate comparable to Maryland's. See `RESEARCH_LOG.md` for the full verification record and what changed. |
| Starter game file built | 2026-09-23 | `kits/kit03-tpt/src/Kit03_ReviewGameStarter.html`, adapted from the already-shipped, design-standard-compliant `kits/tpt-vibe-expedition/src/long-road.html` (rebuilt 2026-09-22 to pass `tpt/intel/GAME-DESIGN-STANDARD.md` in full: mastery tracked per idea, a missed idea returns later and its fix is the loudest moment in the game, a real route-choice decision every turn, no public individual leaderboard, no pixel art). Trimmed from 14 questions to 9 (three ideas × three levels, adding one new level-3 precipitation question) to match the brief's "eight to ten entries" spec for the question block. The teacher-edit comment block was rewritten for a non-coder audience with explicit lab instructions (open, edit, then two AI-prompted changes). Browser-tested with Playwright: zero console/page errors across 40+ turns including a forced wrong-answer path, a forced "missed idea returns and gets fixed" celebration, and a private-window / `localStorage`-blocked simulation. Full test notes in the ship summary. |
| Deck built | 2026-09-23 | `kits/kit03-tpt/src/deck.js`, 34 slides, built to the Kit 1/2 helper pattern verbatim (base/title/kicker/bullets/card/riveraScreen/legendChips/beforeLine). Ms. Rivera continues as the series' running exemplar (Track A, Kit 3 of 20): she builds one multiplication-facts review game through the full describe/build/test/change-one-thing loop (slides 9-13) before the room tries it themselves, matching the KIT_STANDARD lab-exemplar model. Slide-count and structure follow the brief's section (b) table exactly. Font sizes inside cards were raised from a few sub-16pt instances to the KIT_STANDARD 16pt floor during authoring, before any owner review. |
| Facilitator script built | 2026-09-23 | `kits/kit03-tpt/src/Kit03_FacilitatorScript.html` + `.cover.html`, ~10 pp + cover (11 pages total, matches KIT_STANDARD component 3's target), keyed to all 34 slide numbers with stage directions and a running clock. Carries the three script-specific requirements the brief called for: (1) a recovery line and a sub-30-second fix for each of the three predictable breaks (slides 27-28: blank page; changed the wrong thing / questions disappeared), (2) a fully scripted "AI is wrong on purpose" live moment (slide 15: a vague "make my game better" prompt, with the facilitator naming what was missing, plus a stage-direction fallback if the live demo produces something odd rather than merely unhelpful), and (3) a three-minute "write four questions now" beat before the lab (slide 25) for participants who arrive without pre-written questions. "From the founders" note is Why-only (two paragraphs, no stories), identical setup to Kit 1 and Kit 2. |
| Shared template bug found and fixed | 2026-09-23 | `check_footer.py` (added 2026-09-20, after Kit 1/2 shipped) failed on Kit 3's cover page in this build container. Rebuilding Kit 1's and Kit 2's own committed cover sources fresh, in this same container, reproduced the identical failure (confirmed the already-live Kit 1/2 PDFs, read as committed static files, also show a smaller 3pt version of the same overflow — a pre-existing defect nobody had re-rendered against this gate since it was added). Root-caused to `kits/tooling/brand.css`'s `.cover .credit` block being flex-flow-positioned, which let its rendered position drift under small content and font-metric differences across containers. Fixed by taking `.cover .credit` out of the flex flow (`position: absolute`, anchored to a fixed offset from the sheet's bottom edge) so its position is deterministic regardless of what renders above it. Verified with `check_footer.py` (0 collisions) and a full-page screenshot of both the Kit 3 cover and a freshly-rendered Kit 2 cover (both render cleanly, nothing clipped). **This is a shared-template fix; it does not touch or require re-shipping the already-committed, already-live Kit 1 or Kit 2 PDF files**, since those are static binaries unaffected by a CSS source change. Flagged here for whoever next rebuilds Kit 1 or Kit 2's PDFs: the old 3pt overflow in the currently-shipped files is now moot going forward. |
| `check_fonts.py` scope note | 2026-09-23 | `check_fonts.py` flags Inter as a "fallback" font because its `FALLBACK` tuple includes the substring `inter`; that check's `BRAND` tuple (Fredoka/Nunito/LuckiestGuy) is scoped to the Bright Scholar TPT-listing brand, not the Track A Kit-series brand (`kits/tooling/brand.css`, Inter, per `KIT_STANDARD.md`). Confirmed this is not new: running the same check against the already-shipped, currently-committed `Kit01_FacilitatorScript.pdf` and `Kit02_FacilitatorScript.pdf` produces the identical `FALLBACK ... Inter` flag. This is a pre-existing gate/brand scope mismatch that predates Kit 3, not a defect introduced by this build. Not fixed this run (out of scope for a deck+script-only Wednesday); flagged for whoever next touches `check_fonts.py`'s BRAND/FALLBACK tuples. |
| Environment note | 2026-09-23 | `check_overlap.py` could not run in this container: LibreOffice is installed with only `libreoffice-core`/`libreoffice-common` (no Impress/Writer/Draw components), and `apt-get install libreoffice-impress` failed with 404s from the configured mirror. Even a minimal one-slide test `.pptx` and a plain `.txt` file both failed to convert ("source file could not be loaded"), confirming this is a broken/incomplete installation in this specific container, not a problem with the Kit 3 deck. As a substitute, ran a Python `python-pptx`-based bounding-box overlap check across all 34 slides (a weaker proxy: it catches box-level shape collisions but not text that overflows its own box, which is `check_overlap.py`'s specific target). Zero box-level collisions found. **`check_overlap.py` itself must still be run against the finished deck before the 2026-09-30 ship**, in a container where LibreOffice's Impress component is actually installed. |
| Approved | _pending_ | Owner review at the 2026-09-30 ship gate, alongside the seven remaining components. |

**Transparency statement** ships on the References back page (component 9, built next
Wednesday), verbatim, matching Kit 1 and Kit 2.
